{
  "name": "tigerteams",
  "version": "0.1.0",
  "description": "",
  "repository": {
    "type": "git",
    "url": "https://github.com/ughe/cos333.git"
  },
  "scripts": {
    "start": "yarn --cwd backend start",
    "build": "yarn --cwd frontend build",
    "test": "yarn --cwd frontend test",
    "storybook": "yarn --cwd frontend storybook",
    "build-storybook": "yarn --cwd frontend build-storybook",
    "postinstall": "yarn --cwd backend && yarn --cwd frontend",
    "heroku-postbuild": "yarn --cwd frontend build"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "eslintConfig": {
    "extends": "react-app"
  },
  "dependencies": {
    "concurrently": "^4.1.0",
    "serve": "^10.1.2",
    "tigerteams-backend": "file:backend",
    "tigerteams-frontend": "file:frontend"
  }
}
